plugin:
    name: 'briddle.workflow::lang.plugin.name'
    description: 'briddle.workflow::lang.plugin.description'
    author: Briddle
    icon: oc-icon-magic
    homepage: 'https://www.briddle.nl'
permissions:
    workflow:
        tab: 'briddle.workflow::lang.plugin.name'
        label: 'briddle.workflow::lang.permissions.manage'
navigation:
    workflow:
        label: 'briddle.workflow::lang.plugin.name'
        url: briddle/workflow/rules
        icon: icon-magic
        permissions:
            - workflow
        sideMenu:
            rules:
                label: 'briddle.workflow::lang.navigation.rules'
                url: briddle/workflow/rules
                icon: icon-cogs
                permissions:
                    - workflow
            triggers:
                label: 'briddle.workflow::lang.navigation.triggers'
                url: briddle/workflow/triggers
                icon: icon-code
                permissions:
                    - workflow
            actions:
                label: 'briddle.workflow::lang.navigation.actions'
                url: briddle/workflow/actions
                icon: icon-flash
                permissions:
                    - workflow
            workflows:
                label: 'briddle.workflow::lang.navigation.workflows'
                url: /briddle/workflow/workflows
                icon: icon-sitemap
                permissions:
                    - workflow
