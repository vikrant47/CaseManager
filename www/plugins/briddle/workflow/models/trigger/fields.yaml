fields:
    name:
        label: 'briddle.workflow::lang.trigger'
        span: full
        required: 1
        type: text
    type:
        label: 'briddle.workflow::lang.type'
        options:
            cronjob: cronjob
            event: event
            manual: manual
        span: auto
        required: 0
        type: dropdown
    value:
        label: 'briddle.workflow::lang.value'
        span: auto
        type: text
        comment: 'e.g. * * * * * , rainlab.user.register, eloquent.saved: {Author}{Plugin}\Models{Model}'
