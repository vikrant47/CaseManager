fields:
    name:
        label: 'briddle.workflow::lang.action'
        span: auto
        required: 1
        type: text
        comment: 'e.g. ping'
    type:
        label: 'briddle.workflow::lang.type'
        options:
            mail: mail
            webhook: webhook
            log: log
            workflow: workflow
            query: query
        span: auto
        type: dropdown
    value:
        label: 'briddle.workflow::lang.value'
        size: large
        language: php
        span: full
        required: 1
        type: codeeditor
        comment: 'e.g. https://www.briddle.nl, any raw SQL (if you want to use it to mail the result should contain at least a name and email'
